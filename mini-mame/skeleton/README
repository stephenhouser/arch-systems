# mini-mame setup

File Locations:

- ~/.config : contains static configurations for software/games
    - ~/.mame is linked to ~/.config/mame
    - ~/.attract is linked to ~/.config/attract
- .local : contains dynamic high scores, menus, etc.
